when we pop from the stack the prev push to stack needs to be of the corresponding type else false


when we reach the end of the string the len of stack needs to be zero else false (all openings have been closed)


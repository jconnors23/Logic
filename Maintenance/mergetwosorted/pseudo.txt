You are given the heads of two sorted linked lists list1 and list2.

Merge the two lists into one sorted list. The list should be made by splicing together the nodes of the first two lists.

Return the head of the merged linked list.

 

Example 1:


Input: list1 = [1,2,4], list2 = [1,3,4, 5, 6, 7]
Output: [1,1,2,3,4,4]
Example 2:

Input: list1 = [], list2 = []
Output: []
Example 3:

Input: list1 = [], list2 = [0]
Output: [0]


567 123 


Constraints:

The number of nodes in both lists is in the range [0, 50].
-100 <= Node.val <= 100
Both list1 and list2 are sorted in non-decreasing order



steps:

- iterate through list2 and compare to list 1
- adjust list 1 only i.e. place vals of two in 1 
- if val L2 is < val L1, place at node prev to that val in 1, and continue to next val in 2, but start from beginning of L1 again? bc val placed into L1 still needs proper referencing 
- if val L2 is > val l1, place node at node right after that val in 1, continue to next al in 2, start from beginning 
- if either list is empty or we are out of references in 2, return list 1
- check if either list null at beginning, and if so return list that is not emtpy and if both empty return empty